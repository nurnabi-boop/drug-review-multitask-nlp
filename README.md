# drug-review-multitask-nlp
Multi-task NLP on 215k patient drug reviews: sentiment prediction + adverse drug reaction (ADR) extraction. Benchmarks TF-IDF, DistilBERT, and PubMedBERT with SHAP interpretability and a Streamlit safety dashboard.
